var Site = Site || {};		Site.utils = Site.utils || {};	  /**	   * Global Utility functions	   *	   * @module Site.utils	   *//* *	Logs a message to the console * *	@param message String the message to log */Site.log = function(message, colour){	if (window.console){		if(colour){			console.log("%c "+message, 'color:'+colour);		} else {			console.log("",message);		}	}};Site.utils.randomNum = function(min, max){	return Math.floor(Math.random() * (max - min + 1)) + min;};Site.utils.politeLoadImg = function(callback){	var images = document.getElementsByTagName("img");	var numImages = images.length;	var imagesLoaded = 0;	for(var i = 0; i < numImages; i++){	    var newSrc = images[i].getAttribute("data-src");	    if(newSrc){	      images[i].src = newSrc;	      images[i].onload = function(){	        imagesLoaded ++;	        if (imagesLoaded == numImages){	          if(callback){							callback.apply();						}	        }	      };	    }	    else {	      imagesLoaded ++;	      if (imagesLoaded == numImages){						if(callback){							callback.apply();						}	      }	    }	  }}