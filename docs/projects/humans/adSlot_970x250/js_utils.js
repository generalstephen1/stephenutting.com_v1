var Ad = Ad || {};Ad.dom = Ad.dom || {};Ad.config = Ad.config || {};Ad.utils = Ad.utils || {};Ad.timeline = Ad.timeline || {};  /* * * * * * * * * * * * * * * * * * *   *	UTILS   */	/*	 *	Goes to the specified step in the switch statement after the specified timeout	 *	 *	@param step Integer the step to go to	 *	@param duration Integer number of milliseconds to delay before going to the step	 */	Ad.utils.goTo = function(step, duration){		stepTimer = setTimeout(function(){			if (Ad.config.adRunning){				Ad.timeline.doStep(step);			}		}, duration);	}	/*	 *	Logs a message to the console	 *	 *	@param message String the message to log	 */	Ad.log = function(message){		if (Ad.config.debugMode && window.console){			//console.log("[----] " + message);		}	}	/*	 *	Changes the opacity (alpha) of the given dom elements	 *	 *	@param opacity Number the opacity to change all the elements to	 *	@param elemArray Array a list of DOM elements	 */	Ad.utils.alpha = function(opacity, elemArray){		for(var i = 0; i < elemArray.length; i++){			elemArray[i].style.opacity = opacity;		}	}	/*	 *	Hides the selected dom elements	 *	 *	@param elemArray Array a list of DOM elements	 */	Ad.utils.hide = function(elemArray){		for(var i = 0; i < elemArray.length; i++){			elemArray[i].style.display = "none";		}	}	/*	 *	Shows the selected dom elements	 *	 *	@param elemArray Array a list of DOM elements	 */	Ad.utils.show = function(elemArray){		for(var i = 0; i < elemArray.length; i++){			elemArray[i].style.display = "block";		}	}	/*	 *	fadein function which fades in any element by changing opacity	 *	 *	@param elem 			DOMElement 	the element to fade in	 *	@param duration 	Integer 		the amont of milliseconds the fade should last for	 *	@param to					Number 			the ending opacity of the element (default 1)	 */	Ad.utils.fadeIn = function(elem, duration, to) {		var units = duration/20;		var step = 1/units;		if (!to){			to = 1;		}		var i=0;		fade();		function fade(){			elem.style.opacity = i;			if (i<=to){				i += step;				window.setTimeout(fade, 20);			}		}	}	/*	 *	fadeOut function which fades out any element by changing opacity	 *	 *	@param elem 			DOMElement 	the element to fade out	 *	@param duration 	Integer 		the amont of milliseconds the fade should last for	 */	Ad.utils.fadeOut = function(elem, duration) {		var units = duration/25;		var step = 1/units;		var i=1;		fade();		function fade(){			elem.style.opacity = i;			if (i>0){				i -= step;				window.setTimeout(fade, 25);			}		}	}	Ad.utils.randomNum = function(min, max){		return Math.floor(Math.random() * max) + min;	}